{{> menu id="dashboard"}}

<section class="ui segment">
  {{> memberinfo}}
  {{> assessmentdivider}}
  {{> addassessment}}
  {{> assessment-table}}
  {{> goaldivider}}
  {{> addgoal}}
  {{> listgoals}}
</section>